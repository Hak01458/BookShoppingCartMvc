@model Book
@{
    Layout = "_Layout";
}
<div class="container mt-4">
    <h2>Chi tiết sách</h2>

    <form>
        <div class="row">
            <div class="col-md-4">
                <div class="mb-3">
                    <label class="form-label">Cover</label>
                    <div>
                        <img src="@(string.IsNullOrEmpty(Model?.Image) ? "/images/NoImage.png" : $"~/images/{Model.Image}")"
                             class="img-fluid" style="max-height:300px" />
                    </div>
                </div>
            </div>

            <div class="col-md-8">
                <div class="mb-3">
                    <label class="form-label">Tên sách</label>
                    <input class="form-control" value="@(Model?.BookName ?? "")" disabled />
                </div>

                <div class="mb-3">
                    <label class="form-label">Tác giả</label>
                    <input class="form-control" value="@(Model?.AuthorName ?? "")" disabled />
                </div>

                <div class="mb-3">
                    <label class="form-label">Thể loại</label>
                    <input class="form-control"
                           value="@(Model?.Genre?.GenreName ?? Model?.GenreName ?? "Unknown")"
                           disabled />
                </div>

                <div class="mb-3">
                    <label class="form-label">Giá</label>
                    <input class="form-control" value="@(Model?.Price.ToString("F2") ?? "0.00")" disabled />
                </div>

                <div class="mb-3">
                    <label class="form-label">Số lượng</label>
                    <input class="form-control" value="@(Model?.Quantity ?? 0)" disabled />
                </div>

                <div class="d-flex gap-2">
                    <a class="btn btn-secondary" href="javascript:history.back()">Quay lại</a>
                    <a class="btn btn-primary" href="/Cart/AddItem?bookId=@Model?.Id&redirect=1">Thêm vào giỏ hàng</a>
                </div>
            </div>
        </div>
    </form>
</div>